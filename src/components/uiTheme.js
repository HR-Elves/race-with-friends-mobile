import { COLOR } from 'react-native-material-ui';

export default uiTheme = {
  palette: {
      primaryColor: COLOR.teal500,
      accentColor: '#00BFA5'
  },
  toolbar: {
      container: {
          height: 50,
      }
  }
};